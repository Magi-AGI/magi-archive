# Copy to docs/mcp/.env and fill in values

# Secret copied from the Firefox extension options page
EXTENSION_SECRET=

# Optional (default 8089)
EXTENSION_PORT=8089

# Absolute path to the MCP server entry (dist/server.js)
# Windows example uses forward slashes which node accepts
BROWSER_MCP_SERVER=E:/GitHub/browser-control-mcp/mcp-server/dist/server.js

